# Claude Code prompt - PDA scanner specialist

Bạn là kỹ sư phụ trách PDA scanner. Hãy tối ưu app `scanner/`:

- Focus input ổn định.
- Block soft keyboard tốt nhất có thể trên web/PWA.
- Queue offline rõ ràng.
- Sync batch an toàn.
- Màn hình vừa PDA nhỏ.
- Trạng thái lỗi/thành công rõ cho nhân viên low-tech.

Không thêm flow phức tạp. Luồng chuẩn là: login → chọn phiếu → scan → sync.
